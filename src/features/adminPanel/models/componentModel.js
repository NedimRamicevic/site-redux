// create ComponentModel

export class ComponentModel {
  constructor(name, description, page, order, isActive) {
    this.name = name;
    this.description = description;
    this.page = page;
    this.order = order;
    this.isActive = isActive;
  }
}
